#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns string length
 * @s: Input integer pointer
 * Return: Always 0
 */

int _strlen(char *s)
{
	int len;

	len = _strlen(s);
	return (len);
}
