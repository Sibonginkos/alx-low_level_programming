#include "main.h"

/**
 * print_line - draws a straigt line in terminal
 * @n - input character
 * Return: Nothing
 */

void print_line(int n)
{
	int n;
	n = '_';

	if (n > 0)
	{
		_putchar(n'\n'0);
	}
	if else (n <= 0)
		_putchar('\n');
}
