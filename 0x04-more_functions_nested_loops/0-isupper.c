#include "main.h"

/**
 * _isupper - Checks for uppercase characters
 * @c: input character
 * Return: 1 if uppercase otherwise 0
 */

int _isupper(int c)
{
	int c;
	int upper = 0;

	for (c = 'A'; c = 'Z'; i++)
	{
		if (c == c)
			upper = 1;
	}

	return (upper);
}
