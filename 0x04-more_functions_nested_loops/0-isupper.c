#include "main.h"

/**
 * _isupper - Checks for uppercase characters
 * @c - input character
 * Return: 1 if uppercase otherwise 0
 */

int _isupper(int n)
{
	char i;
	int upper = 0;

	for (i = 'A'; i = 'Z'; i++)
	{
		if (i == c)
			upper = 1;
	}

	return (upper);
}
