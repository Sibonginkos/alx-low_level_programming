#include "main.h"

/**
 * print_triangle - Prints triangle
 * @size - Size of triangle
 * Return: Nothing
 */

void print_triangle(int size)
{
	char size;

	size = '#';

	if (size > 0)
		_putchar("#\n");

	else if (n <= 0)
		_putchar('\n');
}
