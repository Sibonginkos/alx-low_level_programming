#include "main.h"

/**
 * print_triangle - Prints triangle
 * @size - Size of triangle
 * Return: Nothing
 */

void print_triangle(int size)
{
	int size;
	size = '#';

	if (size > 0)
		_putchar('#''\n');

	if else (n <= 0)
		_putchar('\n');
}
