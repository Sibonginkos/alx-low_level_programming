#include "main.h"

/**
 * print_square - Prints a square
 * @n - input character
 * Return: Nothing
 */

void print_square(int size)
{
	int size;
	size = '#';

	if (size > 0)
		_putchar('#''\n');

	if else (n <= 0)
		_putchar('n');
}
