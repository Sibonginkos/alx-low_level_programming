#include <stdio.h>

/**
 * main - possible combinatin of single digit numbers
 * Return: Always 0 (Success)
 */

int mai(void)
{
	int i;
	for (i = 0; i < 10; i++)
	{
		putchar("i, ")
	}
	{
		putchar(%i);
	}
	{
		putchar('\n');
	}
	return (0);
}
