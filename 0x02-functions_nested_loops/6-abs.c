#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * int _abs - Print absolute values of intergers
 * Return: Absolute value of r
 */

int _abs(int)
{
	int r;

	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
